
function actionLink(label,fn,kind='light'){return `<button class="proto-btn ${kind} sm" onclick="${fn}">${label}</button>`}
async function jobseekerJobs(){setPage('Job Seeker','Job discovery','Find jobs',`<div class="proto-head"><div><h1>Find your best-fit role</h1><p>Search opportunities and review match-ready job details.</p></div><a class="proto-btn light" href="matching.html">Match insights</a></div><div class="filterbar"><input id="jobq" placeholder="Search title, company or keyword"><input id="jobloc" placeholder="Location"><button class="proto-btn teal" onclick="loadCandidateJobs()">Search jobs</button></div><div id="jobcards" class="job-grid"><div class="proto-panel">Loading opportunities...</div></div>`);loadCandidateJobs()}
async function loadCandidateJobs(){const box=document.getElementById('jobcards');try{let qs=[];if(jobq?.value)qs.push('query='+encodeURIComponent(jobq.value));if(jobloc?.value)qs.push('location='+encodeURIComponent(jobloc.value));const data=arr(await request('/api/jobs'+(qs.length?'?'+qs.join('&'):'')));box.innerHTML=data.length?data.map(j=>`<article class="job-card"><div style="display:flex;justify-content:space-between;gap:12px"><div class="proto-logo">${initials(j.companyName||j.company?.name||'NH')}</div><span class="proto-badge teal">${esc(j.status||'Published')}</span></div><h3>${esc(j.title||'Job')}</h3><div class="muted" style="font-size:12px">${esc(j.companyName||j.company?.name||'Company')} • ${esc(j.locationCity||j.locationCountry||'Flexible')}</div><div class="job-meta"><span class="proto-badge blue">${esc(j.employmentType||'Opportunity')}</span>${j.isRemote?'<span class="proto-badge">Remote</span>':''}</div><p class="muted" style="font-size:12px">${esc((j.description||'').slice(0,130))}${(j.description||'').length>130?'…':''}</p><div class="proto-actions"><button class="proto-btn teal sm" onclick="applyJob('${j.id||j.jobId||''}')">Apply</button><button class="proto-btn light sm" onclick="viewJob('${j.id||j.jobId||''}')">Details</button></div></article>`).join(''):'<div class="proto-panel proto-empty">No jobs matched your filters.</div>'}catch(e){box.innerHTML=`<div class="proto-panel"><b>Could not load jobs</b><p class="muted">${esc(e.message)}</p></div>`}}
async function applyJob(id){if(!id)return toast('Job ID unavailable');const key='web-'+Date.now();try{await request('/api/applications',{method:'POST',body:JSON.stringify({jobId:id,idempotencyKey:key})});toast('Application submitted successfully')}catch(e){toast(e.message)}}async function viewJob(id){try{const d=await request('/api/jobs/'+id);alert(JSON.stringify(d,null,2))}catch(e){toast(e.message)}}
async function applicationsPage(role){const candidate=role==='Job Seeker';setPage(role,candidate?'My applications':'Candidate pipeline',candidate?'Applications':'Candidate ranking',`<div class="proto-head"><div><h1>${candidate?'Application tracker':'Candidate pipeline'}</h1><p>${candidate?'Track each submission and its status history.':'Review applicants, matching context and consent-aware contact.'}</p></div></div><section class="proto-panel"><div class="proto-panel-head"><div><h2>${candidate?'Recent applications':'Applicant queue'}</h2><p>Live data from the application service</p></div><button class="proto-btn light sm" onclick="loadApps('${role}')">Refresh</button></div><div id="apptable">Loading...</div></section>`);loadApps(role)}
async function loadApps(role){const candidate=role==='Job Seeker',box=document.getElementById('apptable');try{let data=candidate?arr(await request('/api/applications/mine')):[];if(!candidate){box.innerHTML=`<div class="proto-notice">Open a vacancy in Vacancies and use its Job ID to load applicants.</div><div class="filterbar" style="margin-top:12px"><input id="appjobid" placeholder="Job ID"><button class="proto-btn teal" onclick="loadEmployerApps()">Load candidates</button></div>`;return}box.innerHTML=`<table class="proto-table"><thead><tr><th>Role</th><th>Status</th><th>Match</th><th>Actions</th></tr></thead><tbody>${data.length?data.map(a=>`<tr><td><b>${esc(a.jobTitle||a.vacancyTitle||'Application')}</b><div class="muted">${esc(a.companyName||'')}</div></td><td><span class="proto-badge blue">${esc(a.status||'Submitted')}</span></td><td>${a.matchScoreSnapshot??'—'}</td><td>${actionLink('History',`showHistory('${a.id||a.jobApplicationId||''}')`)} ${actionLink('Withdraw',`withdrawApp('${a.id||a.jobApplicationId||''}')`,'danger')}</td></tr>`).join(''):'<tr><td colspan="4">No applications yet.</td></tr>'}</tbody></table>`}catch(e){box.innerHTML=esc(e.message)}}
async function loadEmployerApps(){try{const data=arr(await request('/api/applications/jobs/'+appjobid.value));apptable.innerHTML=`<table class="proto-table"><thead><tr><th>Candidate</th><th>Status</th><th>Match</th><th>Actions</th></tr></thead><tbody>${data.map(a=>`<tr><td>${esc(a.candidateName||a.jobSeekerName||'Candidate')}</td><td><span class="proto-badge blue">${esc(a.status||'Submitted')}</span></td><td>${a.matchScoreSnapshot??'—'}</td><td>${actionLink('History',`showHistory('${a.id||a.jobApplicationId||''}',true)`)} ${actionLink('Contact',`contactReq('${a.id||a.jobApplicationId||''}')`,'teal')}</td></tr>`).join('')}</tbody></table>`}catch(e){toast(e.message)}}
async function showHistory(id,employer=false){try{alert(JSON.stringify(await request(`/api/v1/${employer?'employer':'candidate'}/applications/${id}/history`),null,2))}catch(e){toast(e.message)}}async function withdrawApp(id){try{await request(`/api/v1/candidate/applications/${id}/withdraw`,{method:'POST',body:'{}'});toast('Application withdrawn');loadApps('Job Seeker')}catch(e){toast(e.message)}}async function contactReq(id){try{await request(`/api/v1/employer/applications/${id}/contact-request`,{method:'POST',body:'{}'});toast('Contact request sent')}catch(e){toast(e.message)}}
async function profilePage(){setPage('Job Seeker','Skill passport','Skill passport',`<div class="proto-head"><div><h1>Your skill passport</h1><p>Keep structured profile evidence accurate for matching.</p></div><button class="proto-btn teal" onclick="saveProfile()">Save profile</button></div><div class="proto-grid2"><section class="proto-panel"><div class="proto-panel-head"><div><h2>Professional profile</h2><p>Core information used across your account</p></div></div><div id="profileform">Loading...</div></section><section class="proto-panel"><div class="proto-panel-head"><div><h2>Evidence readiness</h2><p>Add skills, education, experience, projects and certifications</p></div></div><div class="proto-row"><div class="proto-logo">SK</div><div><strong>Skills</strong><small>Add structured skill evidence</small></div><button class="proto-btn light sm" onclick="quickEvidence('skills')">Add</button></div><div class="proto-row"><div class="proto-logo">ED</div><div><strong>Education</strong><small>Qualifications and institutions</small></div><button class="proto-btn light sm" onclick="quickEvidence('education')">Add</button></div><div class="proto-row"><div class="proto-logo">EX</div><div><strong>Experience</strong><small>Roles and duration</small></div><button class="proto-btn light sm" onclick="quickEvidence('experience')">Add</button></div><div class="proto-row"><div class="proto-logo">PJ</div><div><strong>Projects</strong><small>Proof of practical work</small></div><button class="proto-btn light sm" onclick="quickEvidence('projects')">Add</button></div><div class="proto-row"><div class="proto-logo">CT</div><div><strong>Certifications</strong><small>Industry credentials</small></div><button class="proto-btn light sm" onclick="quickEvidence('certifications')">Add</button></div></section></div><div id="profiledata" class="proto-panel" style="margin-top:18px"></div>`);loadProfile()}
async function loadProfile(){try{const p=await request('/api/jobseekers/me');profileform.innerHTML=`<div class="form2"><div class="proto-field"><label>Headline</label><input id="pheadline" value="${esc(p.headline||'')}"></div><div class="proto-field"><label>Location</label><input id="plocation" value="${esc(p.location||p.locationCity||'')}"></div></div><div class="proto-field"><label>Professional summary</label><textarea id="psummary">${esc(p.summary||p.professionalSummary||'')}</textarea></div>`;profiledata.innerHTML='<h2>Current backend profile</h2><pre>'+esc(JSON.stringify(p,null,2))+'</pre>'}catch(e){profileform.innerHTML=`<div class="proto-notice">No profile found yet. Create one to begin matching.</div><div class="proto-field" style="margin-top:12px"><label>Headline</label><input id="pheadline"></div><div class="proto-field"><label>Summary</label><textarea id="psummary"></textarea></div>`}}
async function saveProfile(){const body={headline:pheadline?.value||'',summary:psummary?.value||'',location:plocation?.value||''};try{await request('/api/jobseekers/me',{method:'PUT',body:JSON.stringify(body)});toast('Profile saved')}catch(e){try{await request('/api/jobseekers/me',{method:'POST',body:JSON.stringify(body)});toast('Profile created')}catch(x){toast(x.message)}}}
async function quickEvidence(type){const raw=prompt(`Enter JSON for ${type} (advanced backend DTO input):`,'{}');if(!raw)return;try{await request('/api/jobseekers/me/'+type,{method:'POST',body:raw});toast(type+' added')}catch(e){toast(e.message)}}
async function resumesPage(){setPage('Job Seeker','CV Studio','CV Studio',`<div class="proto-head"><div><h1>CV Studio</h1><p>Create, preview, generate and download resume versions.</p></div><button class="proto-btn teal" onclick="newResume()">New CV</button></div><div class="proto-grid2"><section class="proto-panel"><div class="proto-panel-head"><div><h2>Your resumes</h2><p>Manage saved versions</p></div><button class="proto-btn light sm" onclick="loadResumes()">Refresh</button></div><div id="resumelist">Loading...</div></section><section class="resume-sheet" id="resumepreview"><span class="proto-badge teal">PREVIEW</span><h2 style="margin-top:18px">Select a resume</h2><p class="muted">Preview generated resume content here.</p></section></div>`);loadResumes()}
async function loadResumes(){try{const data=arr(await request('/api/resumes'));resumelist.innerHTML=data.length?data.map(r=>`<div class="proto-row"><div class="proto-logo">CV</div><div><strong>${esc(r.resumeName||r.name||'Resume')}</strong><small>${esc(r.templateName||'Template')} • ${esc(r.updatedAtUtc?.slice?.(0,10)||'')}</small></div><button class="proto-btn light sm" onclick="previewResume('${r.id||r.resumeId||''}')">Preview</button><button class="proto-btn teal sm" onclick="generateResume('${r.id||r.resumeId||''}')">Generate</button></div>`).join(''):'<div class="proto-empty">No resumes yet.</div>'}catch(e){resumelist.innerHTML=esc(e.message)}}async function newResume(){const name=prompt('Resume name','My Professional CV');if(!name)return;try{await request('/api/resumes',{method:'POST',body:JSON.stringify({resumeName:name})});toast('Resume created');loadResumes()}catch(e){toast(e.message)}}async function previewResume(id){try{const d=await request('/api/resumes/'+id+'/preview');resumepreview.innerHTML='<span class="proto-badge teal">PREVIEW</span><pre>'+esc(JSON.stringify(d,null,2))+'</pre>'}catch(e){toast(e.message)}}async function generateResume(id){try{await request('/api/resumes/'+id+'/generate',{method:'POST',body:'{}'});toast('Resume generated')}catch(e){toast(e.message)}}
async function candidateMatching(){setPage('Job Seeker','Match insights','Match insights',`<div class="proto-head"><div><h1>Transparent match insights</h1><p>Understand why opportunities align with your profile.</p></div></div><div class="proto-grid2"><section class="proto-panel"><div class="proto-panel-head"><div><h2>Match explorer</h2><p>Use application ID for explanation</p></div></div><div class="filterbar"><input id="matchappid" placeholder="Application ID"><button class="proto-btn teal" onclick="explainMatch()">Explain match</button></div><div id="matchout" class="proto-empty">Choose an application to view category-level explanation.</div></section><section class="callout"><span class="proto-badge teal">RULE-BASED</span><h2 style="margin-top:16px">Explainable by design</h2><p>Match scores are generated from structured skills, experience, education and other configured criteria. The interface exposes the explanation rather than hiding it.</p></section></div>`)}async function explainMatch(){try{const d=await request('/api/matching/applications/'+matchappid.value+'/explain');matchout.innerHTML='<pre>'+esc(JSON.stringify(d,null,2))+'</pre>'}catch(e){toast(e.message)}}
async function privacyPage(role){setPage(role,'Privacy center','Privacy',`<div class="proto-head"><div><h1>Privacy & consent</h1><p>Control preferences and account deletion requests.</p></div></div><div class="proto-grid2"><section class="proto-panel"><div class="proto-panel-head"><div><h2>Privacy preferences</h2><p>Your current settings</p></div><button class="proto-btn light sm" onclick="loadPrivacy()">Refresh</button></div><div id="privacyout">Loading...</div></section><section class="proto-panel"><div class="proto-panel-head"><div><h2>Account deletion</h2><p>Submit a privacy deletion request</p></div></div><div class="proto-field"><label>Reason</label><textarea id="delreason" placeholder="Optional reason"></textarea></div><button class="proto-btn danger" onclick="requestDelete()">Request deletion</button></section></div>`);loadPrivacy()}async function loadPrivacy(){try{const d=await request('/api/v1/privacy/preferences');privacyout.innerHTML='<pre>'+esc(JSON.stringify(d,null,2))+'</pre>'}catch(e){privacyout.innerHTML=esc(e.message)}}async function requestDelete(){try{await request('/api/v1/privacy/deletion-requests',{method:'POST',body:JSON.stringify({reason:delreason.value})});toast('Deletion request submitted')}catch(e){toast(e.message)}}
// Employer
async function companyPage(){setPage('Employer','Company & verification','Company profile',`<div class="proto-head"><div><h1>Company trust profile</h1><p>Maintain company details and verification evidence.</p></div><div class="proto-actions"><button class="proto-btn light" onclick="loadCompany()">Refresh</button><button class="proto-btn teal" onclick="saveCompany()">Save profile</button></div></div><div class="proto-grid2"><section class="proto-panel"><div class="proto-panel-head"><div><h2>Company details</h2><p>Used across vacancies and employer trust</p></div></div><div id="companyform">Loading...</div></section><section class="proto-panel"><div class="proto-panel-head"><div><h2>Verification workflow</h2><p>Upload evidence, submit and track review</p></div></div><div id="verificationbox"><div class="proto-notice">Load your company first.</div></div></section></div>`);loadCompany()}
let currentCompanyId='';async function loadCompany(){try{let c=await request('/api/companies/mine');if(Array.isArray(c))c=c[0]||{};currentCompanyId=c.id||c.companyId||'';companyform.innerHTML=`<div class="proto-field"><label>Company name</label><input id="cname" value="${esc(c.name||'')}"></div><div class="form2"><div class="proto-field"><label>Industry</label><input id="cind" value="${esc(c.industry||'')}"></div><div class="proto-field"><label>Website</label><input id="cweb" value="${esc(c.website||'')}"></div></div><div class="proto-field"><label>Description</label><textarea id="cdesc">${esc(c.description||'')}</textarea></div>`;verificationbox.innerHTML=`<div class="proto-row"><div class="proto-logo">DOC</div><div><strong>Upload company evidence</strong><small>Document upload endpoint</small></div><button class="proto-btn light sm" onclick="uploadCompanyDoc()">Upload</button></div><div class="proto-row"><div class="proto-logo">SUB</div><div><strong>Submit for verification</strong><small>Send evidence to review queue</small></div><button class="proto-btn teal sm" onclick="submitVerification()">Submit</button></div><div class="proto-row"><div class="proto-logo">STA</div><div><strong>Verification status</strong><small>Load current review state</small></div><button class="proto-btn light sm" onclick="verificationStatus()">Check</button></div><pre id="verout">Ready</pre>`}catch(e){currentCompanyId='';companyform.innerHTML=`<div class="proto-notice">No company profile found. Create one below.</div><div class="proto-field" style="margin-top:12px"><label>Company name</label><input id="cname"></div><div class="proto-field"><label>Industry</label><input id="cind"></div><div class="proto-field"><label>Website</label><input id="cweb"></div><div class="proto-field"><label>Description</label><textarea id="cdesc"></textarea></div>`}}
async function saveCompany(){const body={name:cname.value,industry:cind?.value||'',website:cweb?.value||'',description:cdesc?.value||''};try{if(currentCompanyId)await request('/api/companies/'+currentCompanyId,{method:'PUT',body:JSON.stringify(body)});else{const d=await request('/api/companies',{method:'POST',body:JSON.stringify(body)});currentCompanyId=d.id||d.companyId||''}toast('Company profile saved');loadCompany()}catch(e){toast(e.message)}}async function uploadCompanyDoc(){const raw=prompt('Enter document JSON expected by backend (or use All API Endpoints for multipart details):','{}');if(!raw)return;try{await request(`/api/companies/${currentCompanyId}/documents`,{method:'POST',body:raw});toast('Document uploaded')}catch(e){toast(e.message)}}async function submitVerification(){try{await request(`/api/companies/${currentCompanyId}/verification/submit`,{method:'POST',body:'{}'});toast('Verification submitted')}catch(e){toast(e.message)}}async function verificationStatus(){try{verout.textContent=JSON.stringify(await request(`/api/companies/${currentCompanyId}/verification`),null,2)}catch(e){toast(e.message)}}
async function employerJobs(){setPage('Employer','Vacancy workspace','Vacancies',`<div class="proto-head"><div><h1>Vacancy workspace</h1><p>Create and manage opportunities published by your company.</p></div><button class="proto-btn teal" onclick="showCreateJob()">Create vacancy</button></div><div id="jobcreate" class="proto-panel hidden"></div><section class="proto-panel"><div class="proto-panel-head"><div><h2>Your vacancies</h2><p>Live inventory from the jobs service</p></div><button class="proto-btn light sm" onclick="loadEmployerJobs()">Refresh</button></div><div id="employerjobs">Loading...</div></section>`);loadEmployerJobs()}function showCreateJob(){jobcreate.classList.remove('hidden');jobcreate.innerHTML=`<div class="proto-panel-head"><div><h2>Create vacancy</h2><p>Core details; advanced skill arrays can be managed through API console.</p></div></div><div class="form2"><div class="proto-field"><label>Company ID</label><input id="jcompany"></div><div class="proto-field"><label>Title</label><input id="jtitle"></div></div><div class="proto-field"><label>Description</label><textarea id="jdesc"></textarea></div><div class="form2"><div class="proto-field"><label>Location city</label><input id="jcity"></div><div class="proto-field"><label>Employment type</label><input id="jtype" value="FullTime"></div></div><button class="proto-btn teal" onclick="createJob()">Publish vacancy</button>`}async function createJob(){const body={companyId:jcompany.value,title:jtitle.value,description:jdesc.value,locationCity:jcity.value,employmentType:jtype.value};try{await request('/api/jobs',{method:'POST',body:JSON.stringify(body)});toast('Vacancy created');jobcreate.classList.add('hidden');loadEmployerJobs()}catch(e){toast(e.message)}}async function loadEmployerJobs(){try{const data=arr(await request('/api/jobs/mine'));employerjobs.innerHTML=data.length?`<table class="proto-table"><thead><tr><th>Vacancy</th><th>Status</th><th>Location</th><th>Actions</th></tr></thead><tbody>${data.map(j=>`<tr><td><b>${esc(j.title||'Vacancy')}</b></td><td><span class="proto-badge teal">${esc(j.status||'Draft')}</span></td><td>${esc(j.locationCity||'—')}</td><td>${actionLink('Candidates',`location.href='applications.html?job=${j.id||j.jobId||''}'`)} ${actionLink('Close',`closeJob('${j.id||j.jobId||''}')`,'danger')}</td></tr>`).join('')}</tbody></table>`:'<div class="proto-empty">No vacancies yet.</div>'}catch(e){employerjobs.innerHTML=esc(e.message)}}async function closeJob(id){try{await request('/api/jobs/'+id+'/close',{method:'PATCH'});toast('Vacancy closed');loadEmployerJobs()}catch(e){toast(e.message)}}
async function employerMatching(){setPage('Employer','Matching & rankings','Matching',`<div class="proto-head"><div><h1>Candidate matching</h1><p>Rank applicants using the configured rule set and inspect explanations.</p></div></div><div class="proto-grid2"><section class="proto-panel"><div class="proto-panel-head"><div><h2>Vacancy ranking</h2><p>Enter a job ID to load candidate rankings</p></div></div><div class="filterbar"><input id="rankjob" placeholder="Job ID"><button class="proto-btn teal" onclick="loadRankings()">Load rankings</button></div><div id="rankout" class="proto-empty">No ranking loaded.</div></section><section class="callout"><span class="proto-badge teal">TRANSPARENT</span><h2 style="margin-top:16px">No manual score override</h2><p>Employer decisions can shortlist or reject candidates, but the matching explanation remains a separate evidence-based output.</p></section></div>`)}async function loadRankings(){try{const d=arr(await request(`/api/matching/jobs/${rankjob.value}/rankings`));rankout.innerHTML=d.length?d.map((r,i)=>`<div class="proto-row"><div class="proto-logo">${i+1}</div><div><strong>${esc(r.candidateName||r.jobSeekerName||'Candidate')}</strong><small>${esc(r.explanation||'Ranked candidate')}</small></div><span class="proto-score">${r.totalScore??r.matchScore??'—'}%</span></div>`).join(''):'<div class="proto-empty">No rankings.</div>'}catch(e){toast(e.message)}}
async function employerReports(){setPage('Employer','Reports','Reports',`<div class="proto-head"><div><h1>Company reports</h1><p>Monitor vacancy and application activity for your company.</p></div></div><div class="filterbar"><input id="reportcompany" placeholder="Company ID"><button class="proto-btn teal" onclick="loadCompanyReport()">Generate report</button></div><div id="reportout" class="proto-grid2"><section class="proto-panel"><h2>Report ready</h2><p>Enter a company ID to load live metrics.</p></section><section class="proto-panel"><div class="chart-bars"><i style="height:45%"><span>Jobs</span></i><i style="height:75%"><span>Apps</span></i><i style="height:55%"><span>Shortlist</span></i><i style="height:28%"><span>Reject</span></i></div></section></div>`)}async function loadCompanyReport(){try{const d=await request('/api/v1/reports/company/'+reportcompany.value);reportout.innerHTML=`<section class="proto-panel"><h2>Company summary</h2><pre>${esc(JSON.stringify(d,null,2))}</pre></section><section class="proto-panel"><h2>Activity visualization</h2><div class="chart-bars"><i style="height:${Math.min(100,(d.jobs||0)*8+15)}%"><span>Jobs</span></i><i style="height:${Math.min(100,(d.applications||0)*4+15)}%"><span>Apps</span></i><i style="height:${Math.min(100,(d.shortlisted||0)*6+15)}%"><span>Shortlist</span></i><i style="height:${Math.min(100,(d.rejected||0)*6+15)}%"><span>Reject</span></i></div></section>`}catch(e){toast(e.message)}}
// Admin
async function adminUsers(){setPage('Administrator','User management','User accounts',`<div class="proto-head"><div><h1>User accounts</h1><p>Search, inspect and manage platform users.</p></div><button class="proto-btn teal" onclick="loadUsers()">Refresh</button></div><section class="proto-panel"><div class="filterbar"><input id="userq" placeholder="Search loaded users" oninput="filterUsers()"></div><div id="userout">Loading...</div></section>`);loadUsers()}let USER_CACHE=[];async function loadUsers(){try{USER_CACHE=arr(await request('/api/Admin/users'));renderUsers(USER_CACHE)}catch(e){userout.innerHTML=esc(e.message)}}function filterUsers(){const q=userq.value.toLowerCase();renderUsers(USER_CACHE.filter(u=>JSON.stringify(u).toLowerCase().includes(q)))}function renderUsers(data){userout.innerHTML=`<table class="proto-table"><thead><tr><th>User</th><th>Role</th><th>Status</th><th>Actions</th></tr></thead><tbody>${data.map(u=>`<tr><td><b>${esc(u.fullName||((u.firstName||'')+' '+(u.lastName||''))||u.email)}</b><div class="muted">${esc(u.email||'')}</div></td><td>${esc(u.role||'')}</td><td><span class="proto-badge ${String(u.status).toLowerCase().includes('suspend')?'red':'teal'}">${esc(u.status||'Active')}</span></td><td>${actionLink('Open',`viewUser('${u.id||u.userId||''}')`)}</td></tr>`).join('')}</tbody></table>`}async function viewUser(id){try{alert(JSON.stringify(await request('/api/Admin/users/'+id),null,2))}catch(e){toast(e.message)}}
async function adminCompanies(){setPage('Administrator','Company verification','Verification',`<div class="proto-head"><div><h1>Verification queue</h1><p>Review employer evidence before marketplace trust is granted.</p></div><button class="proto-btn teal" onclick="loadPendingCompanies()">Refresh queue</button></div><section id="companyqueue" class="proto-panel">Loading...</section>`);loadPendingCompanies()}async function loadPendingCompanies(){try{const d=arr(await request('/api/v1/admin/companies/pending'));companyqueue.innerHTML=d.length?d.map(c=>`<div class="proto-row"><div class="proto-logo">${initials(c.name||c.companyName)}</div><div><strong>${esc(c.name||c.companyName||'Company')}</strong><small>${esc(c.industry||'')} • Submitted for manual review</small></div><span class="proto-badge orange">Pending</span><button class="proto-btn teal sm" onclick="reviewCompany('${c.id||c.companyId||''}')">Review</button></div>`).join(''):'<div class="proto-empty">Verification queue is clear.</div>'}catch(e){companyqueue.innerHTML=esc(e.message)}}async function reviewCompany(id){try{const d=await request('/api/v1/admin/companies/'+id+'/verification');const decision=prompt('Decision: Approved or Rejected','Approved');if(!decision)return;const notes=prompt('Review notes','Evidence reviewed');await request('/api/v1/admin/companies/'+id+'/review',{method:'POST',body:JSON.stringify({status:decision,notes})});toast('Verification review submitted');loadPendingCompanies()}catch(e){toast(e.message)}}
async function matchingRules(){setPage('Administrator','Matching rules','Matching rules',`<div class="proto-head"><div><h1>Matching rule configuration</h1><p>Inspect the active rule version and update configuration through controlled administration.</p></div><button class="proto-btn teal" onclick="loadRules()">Load active rule</button></div><div class="proto-grid2"><section class="proto-panel"><div id="ruleout">Loading...</div></section><section class="callout"><span class="proto-badge teal">GOVERNANCE</span><h2 style="margin-top:16px">Versioned scoring</h2><p>Changes to matching rules should be explicit and auditable. Individual candidate scores are not manually edited here.</p></section></div>`);loadRules()}async function loadRules(){try{const d=await request('/api/admin/matching-rules/active');ruleout.innerHTML='<pre>'+esc(JSON.stringify(d,null,2))+'</pre><div class="proto-actions"><button class="proto-btn light sm" onclick="editRule()">Edit JSON</button></div>';window.ACTIVE_RULE=d}catch(e){ruleout.innerHTML=esc(e.message)}}async function editRule(){const raw=prompt('Update active rule JSON',JSON.stringify(window.ACTIVE_RULE||{},null,2));if(!raw)return;try{await request('/api/admin/matching-rules/active',{method:'PUT',body:raw});toast('Matching rule updated');loadRules()}catch(e){toast(e.message)}}
async function auditPage(){setPage('Administrator','Audit trail','Audit log',`<div class="proto-head"><div><h1>Audit trail</h1><p>Operational changes and administrative actions.</p></div><button class="proto-btn teal" onclick="loadAudit()">Refresh</button></div><section class="proto-panel"><div id="auditout">Loading...</div></section>`);loadAudit()}async function loadAudit(){try{const d=arr(await request('/api/v1/admin/audit'));auditout.innerHTML=`<table class="proto-table"><thead><tr><th>Action</th><th>Entity</th><th>Actor</th><th>Time</th></tr></thead><tbody>${d.map(x=>`<tr><td>${esc(x.action||'Action')}</td><td>${esc(x.entityType||'')} ${esc(x.entityId||'')}</td><td>${esc(x.actorUserId||'System')}</td><td>${esc((x.createdAtUtc||'').toString().replace('T',' ').slice(0,19))}</td></tr>`).join('')}</tbody></table>`}catch(e){auditout.innerHTML=esc(e.message)}}
async function adminReports(){setPage('Administrator','Platform reports','Reports',`<div class="proto-head"><div><h1>Platform intelligence</h1><p>High-level health of users, companies, vacancies and applications.</p></div><button class="proto-btn teal" onclick="loadPlatformReport()">Refresh</button></div><div id="platformreport">Loading...</div>`);loadPlatformReport()}async function loadPlatformReport(){try{const d=await request('/api/v1/reports/platform');platformreport.innerHTML=`<div class="proto-stats"><div class="proto-stat" style="--stat:#74c9c3"><span>Users</span><b>${d.users??0}</b><small>Platform accounts</small></div><div class="proto-stat" style="--stat:#9cb5e8"><span>Companies</span><b>${d.companies??0}</b><small>Employer profiles</small></div><div class="proto-stat" style="--stat:#b8a3df"><span>Active jobs</span><b>${d.activeJobs??0}</b><small>Published vacancies</small></div><div class="proto-stat" style="--stat:#f1b46e"><span>Applications</span><b>${d.applications??0}</b><small>Candidate submissions</small></div></div><div class="proto-grid2"><section class="proto-panel"><h2>Raw report</h2><pre>${esc(JSON.stringify(d,null,2))}</pre></section><section class="proto-panel"><h2>Platform activity</h2><div class="chart-bars"><i style="height:${Math.min(100,(d.users||0)*2+10)}%"><span>Users</span></i><i style="height:${Math.min(100,(d.companies||0)*6+10)}%"><span>Companies</span></i><i style="height:${Math.min(100,(d.activeJobs||0)*5+10)}%"><span>Jobs</span></i><i style="height:${Math.min(100,(d.applications||0)*3+10)}%"><span>Apps</span></i></div></section></div>`}catch(e){platformreport.innerHTML=esc(e.message)}}
async function adminPrivacy(){setPage('Administrator','Privacy administration','Privacy',`<div class="proto-head"><div><h1>Privacy deletion requests</h1><p>Review user deletion requests with controlled status decisions.</p></div></div><section class="proto-panel"><div class="filterbar"><input id="delid" placeholder="Deletion request ID"><select id="delstatus"><option>Approved</option><option>Rejected</option><option>Completed</option></select><button class="proto-btn teal" onclick="reviewDeletion()">Update status</button></div><p class="muted">Use the All API Endpoints screen to load or inspect additional privacy endpoints.</p></section>`)}async function reviewDeletion(){try{await request(`/api/v1/admin/privacy/deletion-requests/${delid.value}/${delstatus.value}`,{method:'PATCH'});toast('Deletion request updated')}catch(e){toast(e.message)}}
